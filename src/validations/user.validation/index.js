const { userValidation } = require("./user.register.validation");
const {
  userLoginValidation,
 
} = require("./user.login.validation");

module.exports = {
  userValidation,
  userLoginValidation,
 
};
